<nav class="site-nav" aria-label="Primary navigation">
	<div class="site-nav__shell">
		<div class="site-nav__brushes" aria-hidden="true">
			<span class="site-nav__stroke site-nav__stroke--blue"></span>
			<span class="site-nav__stroke site-nav__stroke--purple"></span>
			<span class="site-nav__stroke site-nav__stroke--blue-fine"></span>
			<span class="site-nav__highlight"></span>
		</div>

		<ul class="site-nav__links">
			<li>
				<a href="#about-us">
					<span>About us</span>
				</a>
			</li>
			<li>
				<a href="#our-team">
					<span>Our team</span>
				</a>
			</li>
			<li>
				<a href="#projects">
					<span>Projects</span>
				</a>
			</li>
			<li>
				<a href="#videos">
					<span>Videos</span>
				</a>
			</li>
			<li>
				<a href="#news">
					<span>News</span>
				</a>
			</li>
			<li>
				<a href="#research">
					<span>Research</span>
				</a>
			</li>
		</ul>
	</div>
</nav>

<style>
	.site-nav {
		position: absolute;
		top: 0.65rem;
		left: 50%;
		width: min(var(--site-nav-width, 96vw), 87.5rem);
		--brush-blue: rgb(181 229 242);
		--brush-blue-dark: rgb(128 199 222);
		--brush-purple: rgb(218 222 244);
		--brush-purple-dark: rgb(172 188 226);
		z-index: 2;
		opacity: 0;
		transform: translate(-50%, -0.75rem);
		/* Wait until the logo has completed its movement into the header. */
		animation: reveal-nav 0.72s cubic-bezier(0.2, 0.75, 0.25, 1) var(--logo-settle-end, 1.4s) forwards;
	}

	.site-nav__shell {
		position: relative;
		display: flex;
		align-items: center;
		min-height: clamp(5.25rem, 8vw, 6.75rem);
		box-sizing: border-box;
		padding: 0 0.5rem;
		isolation: isolate;
		filter: saturate(1.04);
	}

	.site-nav__brushes {
		position: absolute;
		inset: 0;
		z-index: 0;
		overflow: visible;
		pointer-events: none;
	}

	.site-nav__brushes::before,
	.site-nav__brushes::after,
	.site-nav__stroke {
		position: absolute;
		inset: 0;
		content: "";
		background:
			repeating-linear-gradient(178deg, transparent 0 8px, rgb(255 255 255 / 10%) 9px 11px, transparent 12px 19px),
			linear-gradient(180deg, var(--brush-blue), var(--brush-blue-dark) 40%, var(--brush-blue) 78%, rgb(214 241 247));
		clip-path: polygon(
			8% 8%, 18% 12%, 29% 10%, 43% 12%, 58% 7%, 71% 9%, 84% 5%,
			91% 12%, 96% 20%, 91% 28%, 97% 35%, 92% 43%, 98% 51%, 91% 59%,
			97% 68%, 91% 76%, 94% 85%, 80% 89%, 66% 92%, 53% 90%, 39% 94%,
			25% 91%, 12% 93%, 5% 87%, 8% 78%, 2% 71%, 8% 63%, 3% 55%,
			8% 47%, 2% 39%, 9% 31%, 4% 23%
		);
	}

	.site-nav__brushes::before {
		transform: rotate(-1.5deg) scale(1.03, 0.94);
		opacity: 0.72;
		filter: blur(1px);
	}

	.site-nav__brushes::after {
		background:
			repeating-linear-gradient(176deg, transparent 0 7px, rgb(255 255 255 / 11%) 8px 10px, transparent 11px 18px),
			linear-gradient(180deg, var(--brush-purple), var(--brush-purple-dark) 42%, var(--brush-purple) 78%, rgb(226 232 249));
		transform: translate(2px, 5px) rotate(1deg) scale(0.98, 0.96);
		opacity: 0.3;
		filter: blur(2px);
	}

	.site-nav__stroke--blue {
		transform: rotate(0.4deg) scale(0.96, 0.91);
		opacity: 0.9;
	}

	.site-nav__stroke--purple {
		inset: 0.2rem -0.2rem 0.1rem 0.15rem;
		background:
			repeating-linear-gradient(176deg, transparent 0 6px, rgb(108 147 195 / 10%) 7px 9px, transparent 10px 17px),
			var(--brush-purple);
		clip-path: polygon(0 10%, 100% 0, 96% 100%, 2% 92%, 6% 62%, 0 38%);
		transform: rotate(-0.7deg) scale(1.02, 0.9);
		opacity: 0.24;
	}

	.site-nav__stroke--blue-fine {
		inset: 0.55rem -0.3rem 0.3rem 0.3rem;
		background:
			repeating-linear-gradient(176deg, transparent 0 5px, rgb(101 171 195 / 23%) 6px 8px, transparent 9px 17px),
			var(--brush-blue);
		clip-path: polygon(0 10%, 100% 0, 96% 100%, 2% 92%, 6% 62%, 0 38%);
		transform: rotate(1.4deg) scale(1.03, 0.88);
		opacity: 0.46;
	}

	.site-nav__highlight {
		position: absolute;
		inset: 0.8rem 0.8rem 0.55rem;
		z-index: 2;
		pointer-events: none;
		opacity: 0.18;
		background: repeating-linear-gradient(
			169deg,
			transparent 0 14px,
			rgb(255 255 255 / 50%) 15px 17px,
			transparent 18px 30px
		);
		mix-blend-mode: screen;
		clip-path: polygon(3% 12%, 97% 4%, 100% 92%, 5% 98%);
	}

	.site-nav__links {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr)) minmax(10.5rem, 12rem) repeat(3, minmax(0, 1fr));
		align-items: center;
		width: 100%;
		min-width: 0;
		gap: 0.2rem;
		margin: 0;
		padding: 0;
		list-style: none;
		position: relative;
		z-index: 3;
	}

	.site-nav__links li {
		position: relative;
		min-width: 0;
		grid-column: auto;
	}

	.site-nav__links li:nth-child(1) {
		grid-column: 1;
	}

	.site-nav__links li:nth-child(2) {
		grid-column: 2;
	}

	.site-nav__links li:nth-child(3) {
		grid-column: 3;
	}

	.site-nav__links li:nth-child(4) {
		grid-column: 5;
	}

	.site-nav__links li:nth-child(5) {
		grid-column: 6;
	}

	.site-nav__links li:nth-child(6) {
		grid-column: 7;
	}

	.site-nav__links a {
		position: relative;
		display: inline-flex;
		justify-content: center;
		align-items: baseline;
		width: 100%;
		box-sizing: border-box;
		padding: 0.72rem 0.7rem 0.68rem;
		color: rgb(32 35 161 / 88%);
		font-family: "Montserrat", sans-serif;
		font-size: clamp(0.72rem, 1vw, 0.98rem);
		letter-spacing: 0.025em;
		line-height: 1;
		text-decoration: none;
		text-transform: uppercase;
		transition: color 0.25s ease, transform 0.25s ease;
	}

	.site-nav__links a::before {
		position: absolute;
		inset: 0.34rem -0.12rem 0.05rem -0.15rem;
		z-index: 0;
		background:
			repeating-linear-gradient(174deg, transparent 0 6px, rgb(255 255 255 / 16%) 7px 9px, transparent 10px 16px),
			linear-gradient(180deg, rgb(232 218 247 / 94%), rgb(199 174 226 / 88%) 58%, rgb(226 210 243 / 92%));
		clip-path: polygon(1% 22%, 17% 9%, 36% 16%, 55% 4%, 79% 12%, 99% 18%, 94% 42%, 100% 74%, 83% 89%, 61% 83%, 44% 96%, 23% 87%, 4% 94%, 8% 61%, 0 44%);
		content: "";
		opacity: 0.72;
		transform: rotate(-2.4deg) scale(1.1, 0.8) translate(-0.1rem, 0.1rem);
		transform-origin: left center;
		transition: opacity 0.25s ease, transform 0.25s ease;
	}

	.site-nav__links a > span {
		position: relative;
		z-index: 1;
	}

	.site-nav__links li:nth-child(2) a::before {
		inset: 0.05rem 0.12rem 0.22rem -0.12rem;
		clip-path: polygon(8% 8%, 31% 3%, 57% 12%, 91% 4%, 97% 29%, 90% 53%, 98% 80%, 69% 94%, 45% 87%, 18% 98%, 4% 71%, 11% 46%);
		opacity: 0.62;
		transform: rotate(1.8deg) scale(0.9, 1.02) translate(0.05rem, -0.1rem);
		transform-origin: right center;
	}

	.site-nav__links li:nth-child(3) a::before {
		inset: 0.28rem -0.18rem 0.12rem 0.08rem;
		clip-path: polygon(0 29%, 21% 13%, 45% 17%, 65% 4%, 100% 17%, 94% 42%, 100% 72%, 74% 83%, 51% 93%, 29% 83%, 4% 97%, 8% 61%);
		opacity: 0.7;
		transform: rotate(-0.8deg) scale(1.12, 0.78) translate(-0.05rem, 0.12rem);
		transform-origin: center bottom;
	}

	.site-nav__links li:nth-child(4) a::before {
		inset: 0.08rem 0.02rem 0.3rem -0.1rem;
		clip-path: polygon(4% 7%, 27% 15%, 48% 6%, 68% 14%, 96% 5%, 91% 31%, 100% 52%, 88% 73%, 96% 93%, 67% 87%, 46% 100%, 24% 87%, 3% 92%, 9% 65%, 0 41%);
		opacity: 0.66;
		transform: rotate(2.2deg) scale(0.96, 0.86) translate(0.08rem, -0.05rem);
		transform-origin: right bottom;
	}

	.site-nav__links li:nth-child(5) a::before {
		inset: 0.22rem -0.12rem 0.1rem 0.1rem;
		clip-path: polygon(12% 3%, 36% 13%, 55% 2%, 78% 16%, 100% 10%, 91% 35%, 98% 59%, 89% 81%, 71% 91%, 47% 84%, 30% 98%, 10% 88%, 3% 62%, 9% 38%);
		opacity: 0.74;
		transform: rotate(-1.6deg) scale(1.06, 0.91) translate(-0.06rem, 0.06rem);
		transform-origin: left bottom;
	}

	.site-nav__links li:nth-child(6) a::before {
		inset: 0.05rem 0.1rem 0.25rem -0.14rem;
		clip-path: polygon(0 12%, 24% 7%, 43% 14%, 64% 3%, 83% 11%, 100% 6%, 94% 32%, 100% 55%, 92% 78%, 73% 91%, 49% 86%, 29% 96%, 7% 89%, 12% 64%, 4% 42%);
		opacity: 0.6;
		transform: rotate(0.7deg) scale(0.92, 1) translate(0.1rem, -0.04rem);
		transform-origin: center top;
	}

	.site-nav__links a::after {
		position: absolute;
		bottom: 0.28rem;
		left: 0.65rem;
		width: 0.8rem;
		height: 1px;
		background: var(--brush-blue);
		content: "";
		opacity: 0.85;
		z-index: 2;
		transform: rotate(-1deg);
		transition: width 0.3s ease, opacity 0.3s ease, background 0.3s ease;
	}

	.site-nav__links a:hover,
	.site-nav__links a:focus-visible {
		color: var(--blue);
		transform: translateY(-0.18rem) rotate(-1deg);
	}

	.site-nav__links li:nth-child(even) a:hover,
	.site-nav__links li:nth-child(even) a:focus-visible {
		transform: translateY(-0.18rem) rotate(1deg);
	}

	.site-nav__links a:hover::after,
	.site-nav__links a:focus-visible::after {
		width: calc(100% - 1.3rem);
		opacity: 1;
		background: var(--brush-blue-dark);
	}

	.site-nav__links a:focus-visible {
		outline: 2px dashed var(--brush-blue-dark);
		outline-offset: 0.2rem;
	}

	@keyframes reveal-nav {
		from {
			opacity: 0;
			transform: translate(-50%, -0.75rem) rotate(-0.5deg);
		}
		to {
			opacity: 1;
			transform: translate(-50%, 0) rotate(0);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.site-nav {
			animation: none;
			opacity: 1;
			transform: translate(-50%, 0);
		}

		.site-nav__links a {
			transition: none;
		}

		.site-nav__links a::before {
			transition: none;
		}

		.site-nav__links a:hover,
		.site-nav__links a:focus-visible {
			transform: none;
		}
	}

	@media (max-width: 56rem) {
		.site-nav {
			top: 0.55rem;
			width: calc(100% - 1rem);
		}

		.site-nav__shell {
			display: block;
			min-height: 9rem;
			padding: 4.5rem 0.7rem 0.5rem;
		}

		.site-nav__links {
			display: grid;
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 0.15rem 0.75rem;
		}

		.site-nav__links li:nth-child(n) {
			grid-column: auto;
		}
	}

	@media (max-width: 36rem) {
		.site-nav {
			top: 0.45rem;
			width: calc(100% - 0.75rem);
		}

		.site-nav__shell {
			min-height: 12rem;
			padding: 4.4rem 0.45rem 0.45rem;
		}

		.site-nav__links {
			grid-template-columns: repeat(2, minmax(0, 1fr));
			gap: 0.1rem 0.55rem;
		}

		.site-nav__links a {
			gap: 0.3rem;
			padding: 0.58rem 0.2rem 0.5rem;
			font-size: 0.68rem;
		}

		.site-nav__links a::after {
			left: 0.2rem;
		}
	}
</style>
